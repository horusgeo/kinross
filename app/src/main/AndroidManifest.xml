<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="horusgeo.eco101">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="Início"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NewSessionActivity"
            android:label="Selecione a(s) sessão(ões):"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ContinueSessionActivity"
            android:label="@string/title_activity_continue_session"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CloseSessionActivity"
            android:label="@string/title_activity_close_session"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MapActivity"
            android:label="@string/title_activity_map"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>